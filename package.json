{
  "name": "secure-status-portal",
  "version": "1.0.0",
  "description": "CYSE 411 Unit 1.2 + 1.3 Assignment",
  "type": "commonjs",
  "scripts": {
    "test": "jest --runInBand",

    "test:sanitize": "jest sanitizeUsername --runInBand",
    "test:dom": "jest renderNotifications --runInBand",
    "test:json": "jest parseProfileJson --runInBand",
    "test:fetch": "jest fetchUserProfile --runInBand",
    "test:storage": "jest storage --runInBand",
    "test:access": "jest computeAccessStatus --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jsdom": "^24.0.0"
  }
}
